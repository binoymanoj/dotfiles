#!/bin/bash

# script for opening youtube(home page) in a new tab (brave browser) # you can change the browser of choice

# Brave profile
PROFILE="Default"
URL="https://www.youtube.com"

# Open the URL with Brave using the Default profile
brave --profile-directory="$PROFILE" "$URL" &
